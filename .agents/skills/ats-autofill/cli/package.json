{
  "name": "ats-autofill",
  "version": "1.0.0",
  "description": "CLI for prefilling US job application forms (Greenhouse, Lever, Ashby, Workday, generic). Never submits — fills, screenshots, and hands the browser to a human for review.",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "ats-autofill": "src/cli.ts"
  },
  "scripts": {
    "start": "node --experimental-strip-types src/cli.ts",
    "test": "bun test --timeout 30000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/bun": "latest"
  }
}
